#ifndef _ELEMENT
#define _ELEMENT

typedef int Elem;

//On définir le type de l'élément dans un module a part permet de n'avoir que ce fichier à modifier si l'on souhaite changer le type des variables à stocker.

void affichageElement(const Elem & e);

#endif
